# Copy to .env and fill in. Provided by the challenge.
# (Voice and model live in the agent JSON, e.g. example_flow.json, not here.)
OPENAI_API_KEY=
# Optional model used by POST /api/copilot/propose.
OPENAI_MODEL=gpt-4o-mini
ELEVENLABS_API_KEY=
