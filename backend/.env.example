# Copy to .env and fill in. Provided by the challenge.
# The voice-agent runtime model still lives in the agent JSON. This model is
# only for call review and Evidence-to-Flow proposals.
OPENAI_API_KEY=
# Model used by the call reviewer and Evidence-to-Flow Copilot.
OPENAI_MODEL=gpt-5.6-luna
ELEVENLABS_API_KEY=
